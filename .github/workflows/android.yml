name: Build Debug APK

on:
  push:
    branches: [ main ]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build Debug APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Check Gradle Wrapper files
        run: |
          echo "Working directory is: $(pwd)"
          echo "Listing root contents:"
          ls -R . | sed 's/^/  /'
          echo ""
          echo "Specifically, contents of gradle/wrapper/:"
          ls -l gradle/wrapper/ || echo "⛔ gradle/wrapper/ not found"

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle'

      - name: Grant execute permission for gradlew
        run: chmod +x ./gradlew

      - name: Build Debug APK
        run: |
          echo "About to run: ./gradlew clean assembleDebug"
          ./gradlew clean assembleDebug

      - name: Upload Debug APK Artifact
        if: ${{ !github.head_ref }}
        uses: actions/upload-artifact@v4
        with:
          name: app-debug
          path: app/build/outputs/apk/debug/app-debug.apk

      - name: Create GitHub Release and Upload APK
        if: ${{ !github.head_ref }}
        uses: softprops/action-gh-release@v2
        with:
          tag_name: debug-${{ github.run_number }}
          name: Debug APK # Release name
          body: Debug APK generated from GitHub Actions run # Description
          draft: false
          prerelease: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload APK to Release
        if: ${{ !github.head_ref }}
        uses: softprops/action-gh-release@v2
        with:
          files: app/build/outputs/apk/debug/app-debug.apk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
